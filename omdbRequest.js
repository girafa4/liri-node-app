// Basic Node application for requesting data from the OMDB we
//Here we incorporate the "request" npm package
var request = requeire ("request");

//  We then run the rquest module on a URL with JSON
request(http;)


//If there were no errors and the response code was 200 (ie... the request was succesful)
 if (!error)